"""The use cases: `plan`, `sync`, `verify`, `trace`.

One pipeline, in `pipeline.py`, and thin commands over it. Two implementations
of the same walk is exactly how a dry run stops predicting the real one.
"""

from __future__ import annotations
