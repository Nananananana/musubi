__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/
.coverage
.hypothesis/
htmlcov/

# musubi's output is the owner's documents, cleaned. A sync run writes the
# corpus itself, and a trace map says which byte of which private file every
# character came from. Neither belongs in a commit. See docs/threat-model.md
# once it exists; until then treat everything below as if it were the notes.
out/
synced/
*.trace.json
*.manifest.json
